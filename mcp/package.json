{
    "name": "@solidtime/mcp-server",
    "version": "0.1.0",
    "description": "MCP server for the solidtime API — time entries, projects, tasks, clients, tags, organizations and members",
    "license": "AGPL-3.0-or-later",
    "type": "module",
    "bin": {
        "solidtime-mcp": "dist/index.js"
    },
    "files": [
        "dist"
    ],
    "engines": {
        "node": ">=20"
    },
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "start": "node dist/index.js",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "^1.12.1",
        "zod": "^3.25.0"
    },
    "devDependencies": {
        "@types/node": "^22.15.0",
        "typescript": "^5.8.3"
    }
}
